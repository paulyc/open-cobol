@p The @syntaxidx{REEL}, @syntaxidx{LOCK} and @syntaxidx{NO REWIND} clauses are syntactically recognized but are otherwise non-functional, except for the @syntax{CLOSE@dots{}NO REWIND} statement, which will generate a file status of 07 rather than the usual 00 (but take no other action).